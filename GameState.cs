namespace Tap.Hangman.App
{ // Enumerarea pentru starea jocului
    public enum GameState
    {
        Won, // Jocul a fost câștigat
        Lost, // Jocul a fost pierdut
        InProgress// Jocul este în desfășurare
    }
}

